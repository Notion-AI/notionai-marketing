const state = () => ({
  data: {},
})

const getters = {
}

const actions = {
}

const mutations = {
  SET_DATA (state, data) {
    state.data = data
  }
}

export default {
  state,
  getters,
  mutations,
  actions
}